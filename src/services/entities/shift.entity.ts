export interface ResponseShift {
  msg:                string;
  success:            string;
  data:               Shift[];
}

export interface Shift {
  id:               string;
  name:             string;
}
