import type { CustomFlowbiteTheme } from 'flowbite-react'

export const customTheme: CustomFlowbiteTheme = {
  button: {
    color: {
      primary: 'bg-primary enabled:hover:bg-primary enabled:hover:bg-opacity-90',
    },
  },
};
